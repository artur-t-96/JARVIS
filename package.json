{
  "name": "jarvis-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trwałe wykonywanie zadań z kontrolą uprawnień, zgodami i dowodami.",
  "engines": {
    "node": ">=22.18.0 <23"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build",
    "demo": "tsx scripts/demo.ts",
    "format": "prettier --write src tests scripts public docs .github README.md AGENTS.md package.json tsconfig.json",
    "format:check": "prettier --check src tests scripts public docs .github README.md AGENTS.md package.json tsconfig.json"
  },
  "dependencies": {
    "fastify": "^5.6.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "prettier": "^3.9.6",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
